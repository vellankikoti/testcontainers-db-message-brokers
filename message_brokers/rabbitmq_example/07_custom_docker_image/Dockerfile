# Dockerfile for Custom RabbitMQ Image
FROM rabbitmq:3.9-management

# Copy custom configuration files if needed
# COPY rabbitmq.conf /etc/rabbitmq/rabbitmq.conf

# Expose the management plugin port
EXPOSE 15672
